#ifndef SIM_HPP
#define SIM_HPP
#include <string>

struct simulador{
    lerArquivo(std::string nomeArquivo);
    rodarComando(std::string estrutura, std::string operacao, int parametro, int parametro2);
};


#endif
